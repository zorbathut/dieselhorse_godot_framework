pipeline {
    agent any
    
    stages {
        stage('Build Docker Image') {
            steps {
                script {
                    // Execute the shell script to build the Docker image
                    sh 'build/godot-build-containers/build.sh'
                }
            }
        }
    }
}
