pipeline {
    agent any
    
    stages {
        stage('Build Docker Image') {
            steps {
                docker.image("quay.io/podman/stable:v5.0.2-immutable").inside {
                    sh 'build/godot-build-containers/build.sh'
                }
            }
        }
    }
}
